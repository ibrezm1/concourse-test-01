---
platform: linux

image_resource:
  type: docker-image
  source: {repository: python}

inputs:
  - name: 01-pipe-git

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    env
    find .
    python ./01-pipe-git/python-version.py
    pwd

#run:
#  path: python
#  args: ["./01-pipe-git/python-version.py"]